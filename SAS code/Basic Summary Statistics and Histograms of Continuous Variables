/* 
 * 
 * Task code generated by SAS Studio 3.8   
 * 
 */ 
 
ods noproctitle; 
ods graphics / imagemap=on; 
 
proc means data=WORK.CHURN chartype mean std min max median n nmiss vardef=df  
		qmethod=os; 
	var tenure MonthlyCharges TotalCharges; 
run; 
 
proc univariate data=WORK.CHURN vardef=df noprint; 
	var tenure MonthlyCharges TotalCharges; 
	histogram tenure MonthlyCharges TotalCharges; 
run;
