/*
 *
 * Task code generated by SAS Studio 3.8  
 *
 */

Title;
ods noproctitle;
ods graphics / imagemap=on;

proc glm data=WORK.CHURN_NUMERIC;
	class InternetService;
	model Churn_num=InternetService;
	means InternetService / hovtest=levene welch plots=none;
	lsmeans InternetService / adjust=tukey pdiff alpha=.05;
	run;
quit;
